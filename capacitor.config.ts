import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Pera Consultores',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
